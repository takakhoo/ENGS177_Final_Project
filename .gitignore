__pycache__/
*.pyc
*.pyo
.venv/
.ipynb_checkpoints/
.DS_Store
*.log
# Note: data/raw/*.csv, data/processed/*.pkl, data/processed/monthly.csv,
# and results/Q_star.npy are intentionally COMMITTED so teammates can run
# the full pipeline end-to-end without first hitting FRED/Yahoo. They are
# still reproducible via experiments/01_fetch_data.py if you ever need to
# refresh them.
